SAFETY CHECK FIRST:

Before implementing, please analyze and report findings:

1. Does POST /api/combat/enemy-turn already exist?
   grep -rn "enemy.turn\|enemy_turn" backend/app/api/combat.py backend/app/api/
   (Phase 8B already added this endpoint — verify it exists and what it returns)

2. Does the frontend already have enemy turn button?
   grep -rn "enemy.turn\|enemy_turn\|Tura wroga\|enemyTurn" frontend/js/
   (Phase 8B added combat_panel.js with attack/flee/item buttons — check if enemy turn button is there)

3. Where is combat panel rendered after player turn?
   grep -rn "combat_panel\|combatPanel\|checkAndRestoreCombat\|loadTurns" frontend/js/
   (Phase 8B: combat_panel.js handles combat UI — check if enemy turn button is already part of it)

4. Is debounce / double-click protection already in place?
   grep -rn "debounce\|disabled\|isLoading\|pendingRequest" frontend/js/combat_panel.js

5. What does enemy-turn endpoint currently return?
   Read the full POST /api/combat/enemy-turn handler in backend/app/api/combat.py

STOP — report findings before implementing.
Phase 8B delivered combat UI (step 8.9 in original roadmap) — most of this may already exist.

---

IMPORTANT context from repo (Phase 8B, verified):

Phase 8B already delivered:
- POST /api/combat/enemy-turn endpoint in backend/app/api/combat.py
- combat_panel.js with attack/flee buttons
- Enemy HP bar in combat UI
- GM roll bubble (live SSE + F5 persistence)
- combat_panel.js distinguishes: pudło / unik / nat1 in messages
- Loki: combat_gm_roll_emit verified working

Files to check (NOT routes.py — that file may not exist):
- backend/app/api/combat.py  ← enemy-turn endpoint
- frontend/js/combat_panel.js  ← combat UI + buttons

DO NOT create a new endpoint if /api/combat/enemy-turn already exists.
DO NOT add enemy turn button if combat_panel.js already has it.

---

IF GAPS FOUND, implement only what is missing:

GAP A — Enemy turn button missing or not showing after player turn:
  In combat_panel.js, after player action completes:
  - Check: is_combat_active AND current_turn != "player"
  - Show button: "⚔️ Tura wroga (kliknij aby kontynuować)"
  - On click: POST /api/combat/enemy-turn
  - Disable button immediately on click (prevent double-click)
  - Re-enable / remove after response received

GAP B — Enemy turn endpoint missing required fields in response:
  POST /api/combat/enemy-turn should return:
    {
      "rolls": [...],           ← roll card data for display
      "narrative": "...",       ← GM narration of enemy attack
      "combat_status": "active" | "ended" | "victory" | "player_dead"
    }
  If response is missing combat_status → frontend cannot determine next state.

GAP C — No debounce / double-click guard:
  In combat_panel.js button handler:
    button.disabled = true  (or add CSS class "loading")
    after response: remove button or re-render combat panel

---

BEFORE IMPLEMENTING, verify:

1. What buttons are currently in combat_panel.js?
   cat frontend/js/combat_panel.js | grep -A3 "button\|btn\|click"

2. What does the enemy-turn endpoint response look like?
   Read combat.py POST enemy-turn handler — what JSON does it return?

3. Is there a current_turn field in GET /api/combat/state or similar?
   grep -rn "current_turn\|combat.*state\|getCombatState" frontend/js/ backend/app/api/

If all gaps already handled → report DONE.

---

ACCEPTANCE CRITERIA:

- [ ] POST /api/combat/enemy-turn endpoint exists and returns rolls + narrative + combat_status
- [ ] Enemy turn button shown in combat panel when current_turn != "player"
- [ ] Button disabled immediately on click (no double-trigger)
- [ ] Button removed / panel refreshed after enemy turn response
- [ ] combat_status "player_dead" triggers death screen (Phase 7.6 / death_screen.js)
- [ ] combat_status "victory" triggers loot popup (Phase 8C)
- [ ] No duplicate buttons shown at same time
- [ ] No existing tests broken: python -m pytest tests/test_phase8_combat.py -v
- [ ] Manual test on 192.168.1.61: full round player → enemy turn button → enemy attacks → next player turn

FILES TO MODIFY (only if gaps confirmed):
- backend/app/api/combat.py        (GAP B — response fields)
- frontend/js/combat_panel.js      (GAP A — button, GAP C — debounce)

DO NOT modify:
- backend/app/api/routes.py (file may not exist — use combat.py)
- backend/prompts/system_prompt.txt
- active_combat schema



=============== AFTER REPORT ===============
Przeczytałem **`step_5.1_enemy_turn_button.txt`** i sprawdziłem **`combat.py`**, **`combat_input.js`**, **`combat_panel.js`**.

## Czy są **blockery** (czyli: nie da się domknąć kroku bez decyzji / dużej pracy)?

**Na działającą turę wroga i request — nie.** Endpoint i front już to obsługują.

**Na 100% zgodność z checklistą w pliku (słowa + kształt JSON) — tak, to są „miękkie” blockery / rozjazdy specyfikacji**, nie awaria.

---

### Co jest w repo (zgodne z celem kroku)

| Punkt z dokumentu | Stan |
|-------------------|------|
| **`POST .../combat/enemy-turn`** | Jest w **`combat.py`** — woła `resolve_attack(..., attacker="enemy")`, potem `advance_turn` / `combat_state`. |
| **Wywołanie z frontu** | **`combat_input.js`**: przy `status === "active"` i **`current_turn !== "player"`** blokuje composer i woła **`fetch(.../enemy-turn)`**; **`_enemyTurnInFlight`** = ochrona przed podwójnym odpaleniem (rodzaj debounce). |
| **Panel** | **`combat_panel.js`**: overlay **„ENEMY TURN”** (`#combat-enemy-overlay`), pokazywany gdy tura nie gracza; **`_busy`** przy akcjach z panelu. |

---

### Rozjazdy względem dokumentu (tu „blocker” = spec vs kod)

1. **GAP B / acceptance:** dokument chce w odpowiedzi m.in. **`rolls`**, **`narrative`**, **`combat_status`**.  
   Backend zwraca **bezpośrednio wynik `resolve_attack`** + **`advance_turn`** + **`combat_state`** — **nie ma** tego ułożonego obiektu.  
   Front **i tak** opiera się na **`combat_state`**, **`player_incapacitated`**, itd. → **brak blokeru na działanie**, o ile nie traktujemy acceptance dosłownie.

2. **GAP A:** dokument opisuje **osobny przycisk** „⚔️ Tura wroga (kliknij…)”.  
   W UI jest **overlay** „ENEMY TURN” i **automatyczne** wywołanie enemy-turn z **`combat_input`** (nie ten sam UX co w tekście kroku 5.1). → **rozjazd opisowy**, nie brak mechaniki.

3. **Śmierć / zwycięstwo / loot** z pola `combat_status` — w praktyce steruje **`combat_state`** i istniejące callbacki (`render`, `player_incapacitated`, victory flow w panelu). Pełna zgodność z linijkami 94–95 acceptance wymaga **osobnej weryfikacji ścieżek** w `death_screen.js` / loot — z pliku nie wynika twardy błąd w kodzie walki.

---

### Werdykt

- **Bloker „nie da się zrobić enemy turn w grze”:** **nie.**  
- **Bloker „trzeba koniecznie zmienić kod zanim przejdziemy dalej”:** **nie**, o ile akceptujecie obecny kontrakt API i UX (auto + overlay zamiast dokładnego przycisku z tekstu).  
- **Bloker „acceptance z `step_5.1` jest w 100% odhaczalna bez zmian”:** **tak**, jeśli ktoś trzyma się **dosłownego** JSON-a i polskiego przycisku — wtedy to **luki do dopisania** (API wrapper + kopia przycisku / kopia tekstu), a nie że coś „nie działa”.

Jeśli chcesz, mogę w kolejnym kroku **wdrożyć tylko dopasowanie do acceptance** (np. `combat_status` + `rolls` w odpowiedzi `enemy-turn`) **albo** zaktualizować **`step_5.1`** do realiów repo — napisz którą stronę wybierasz.