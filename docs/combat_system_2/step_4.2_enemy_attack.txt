SAFETY CHECK RESULT: ✅ MOSTLY DONE — one alias to add

Verified in combat_service.py (2026-04-24):
- resolve_attack(attacker="enemy") EXISTS with full enemy turn logic
- Enemy attack: d20 + attack_bonus vs player AC/DEF (_player_ac_from_sheet) — EXISTS
- Player HP + log_combat_turn(actor="enemy") + end_combat("player_dead") — ALL EXIST
- advance_turn() in POST .../combat/enemy-turn (combat.py) — EXISTS
- Manual enemy turn trigger (button, Opcja B) — unchanged ✅

DESIGN DECISION (2026-04-24) — player defense mechanic:
  Spec draft said: player dodge roll d20 + DEX mod (mirror of enemy dodge)
  Decision:        KEEP static AC/DEF for player defense — INTENTIONAL ASYMMETRY
  Rationale: player actively rolls on their attack turn; enemy attack vs static AC
  is standard RPG design (D&D model), simpler UX, no test rewrite needed.
  GAP A: CLOSED — no code change.

---

IMPLEMENTATION TASK (one change only):

Add resolve_enemy_attack() alias in combat_service.py:

  def resolve_enemy_attack(campaign_id: int, roll_result: int = None, raw_d20: int = None):
      """Thin alias for resolve_attack(attacker='enemy'). roll_result ignored."""
      return resolve_attack(campaign_id, attacker="enemy")

Add one test: test_resolve_enemy_attack_alias_matches_resolve_attack

---

ACCEPTANCE CRITERIA:
- [x] resolve_attack(attacker="enemy") — full enemy turn
- [x] Player defense = static AC/DEF — intentional, not a bug
- [x] Player HP, log, death detection, advance_turn — all exist
- [x] resolve_enemy_attack() alias added
- [x] Alias test passes
- [x] python -m pytest tests/test_phase8_combat.py -v — no regressions

FILES: backend/app/services/combat_service.py only
DO NOT modify: player defense mechanic, enemy trigger, schema, system_prompt.txt



=================AFTER REPORT================
### Zrobione (krok 4.2 — bez blokerów wg aktualnego dokumentu)

Dokument przyjmuje **AC/DEF gracza** przy ataku wroga i **tylko alias** w kodzie.

**`combat_service.py`**
- **`resolve_enemy_attack(campaign_id, roll_result=None, raw_d20=None)`** → wywołuje **`resolve_attack(campaign_id, 0, attacker="enemy", raw_d20=None)`**; argumenty `roll_result` / `raw_d20` są ignorowane (jak w opisie).

**`tests/test_phase8_combat.py`**
- **`test_resolve_enemy_attack_alias_calls_resolve_attack`** — mock `resolve_attack`, sprawdzenie `assert_called_once_with(42, 0, attacker="enemy", raw_d20=None)`.

**`step_4.2_enemy_attack.txt`**
- Zaznaczone acceptance `[x]` dla aliasu, testu i pytest.

### Pytest (.61)

`tests/test_phase8_combat.py`: **29 passed**.

### Docker

Restart / rebuild backendu w kontenerze po wdrożeniu **`combat_service.py`**.