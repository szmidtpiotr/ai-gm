SAFETY CHECK FIRST:

Before implementing, please analyze and report findings:

1. Is enemy death detection already in _persist_combatants_and_maybe_end()?
   grep -n "hp_current.*<=.*0\|dead\|enemy_dead\|victory" backend/app/services/combat_service.py
   (Phase 8A: likely already marks enemies dead and calls end_combat("victory"))

2. Does game_engine.resolve_enemy_loot already exist?
   grep -rn "resolve_enemy_loot\|list_loot_entries\|loot_table_key" backend/app/
   Read the full function — what does it return?

3. Are loot tables in DB schema (migrations_admin.py)?
   grep -n "game_config_loot_tables\|game_config_loot_entries" backend/app/services/migrations_admin.py
   ✅ CONFIRMED EXISTS — schema already present

4. Do game_config_enemies rows have loot_table_key and drop_chance?
   SELECT key, loot_table_key, drop_chance FROM game_config_enemies LIMIT 5;

5. Is loot_pool column in active_combat?
   pragma table_info(active_combat);
   grep -n "loot_pool" backend/app/services/combat_service.py

STOP — report findings before implementing.
If resolve_enemy_loot + loot_pool already wired into combat → audit only, fill gaps.

---

IMPORTANT context (confirmed 2026-04-24):

Schema status:
- game_config_loot_tables + game_config_loot_entries EXIST (migrations_admin.py)
- game_engine.resolve_enemy_loot() EXISTS — uses list_loot_entries + loot_table_key
- Schema migration: use auto-migrate pattern (CREATE TABLE IF NOT EXISTS in combat_service.py
  or existing DB init — Opcja B, consistent with rest of codebase)
- Loot v1: SIMPLIFIED — use existing game_engine.resolve_enemy_loot() as-is,
  DO NOT redesign loot tables, DO NOT add weighted random beyond what already exists

Column / term mapping (use EXISTING names):
  phase='victory'  → status='ended' + ended_reason='victory'  (consistent with player_dead)
  combat_status    → ended_reason in active_combat
  loot_pool        → may not exist yet as column (verify step 5)

DO NOT add "phase" column.
DO NOT write a new loot system — reuse game_engine.resolve_enemy_loot().
DO NOT hardcode item lists — all loot comes from game_config_loot_entries (admin-managed).

---

IF GAPS FOUND, implement only what is missing:

GAP A — loot_pool column missing in active_combat:
  Add to DB init / auto-migrate in combat_service.py:
    ALTER TABLE active_combat ADD COLUMN loot_pool TEXT DEFAULT '[]';
  (only if pragma table_info confirms it's missing)

GAP B — enemy death not marking "dead" flag in combatants JSON:
  In _persist_combatants_and_maybe_end() or resolve_attack():
  for enemy in combatants where type != "player":
      if enemy["hp_current"] <= 0 and not enemy.get("dead"):
          enemy["dead"] = True
          log_combat_turn(actor=enemy["id"], event_type="death",
                          narrative=f"{enemy.get('label','Wróg')} pada...")

GAP C — loot not generated on enemy death:
  After marking enemy dead:
    if enemy.get("loot_table_key"):
        drop_chance = enemy.get("drop_chance", 0.5)
        if random.random() <= drop_chance:
            items = game_engine.resolve_enemy_loot(enemy["loot_table_key"])
            loot_pool.extend(items)

  After all enemies processed:
    UPDATE active_combat SET loot_pool = json.dumps(loot_pool) WHERE campaign_id = ?

GAP D — end_combat("victory") not saving loot_pool before close:
  In end_combat(campaign_id, "victory"):
    SET status='ended', ended_reason='victory', loot_pool=json.dumps(current_loot_pool)
  Consistent with player_dead path from step 6.2.

---

BEFORE IMPLEMENTING, verify:

1. Read full _persist_combatants_and_maybe_end() body.
   Does it already call resolve_enemy_loot? Already write to loot_pool?

2. What does game_engine.resolve_enemy_loot() return?
   List of dicts? [{item_key, item_label, quantity}]?
   Confirm shape before storing in loot_pool JSON.

3. In tests — is resolve_enemy_loot already mocked?
   grep -n "resolve_enemy_loot\|mock.*loot" tests/test_phase8_combat.py
   If yes: keep mock pattern, add assertions on loot_pool content.

If all gaps already handled → report DONE.

---

ACCEPTANCE CRITERIA:

- [ ] enemy hp_current <= 0 → enemy["dead"] = True in combatants JSON
- [ ] Death logged in combat_turns (Polish narrative: "{label} pada...")
- [ ] drop_chance roll: random.random() <= drop_chance
- [ ] Loot from game_config_loot_entries via resolve_enemy_loot() (admin-managed, no hardcoding)
- [ ] Loot stored in active_combat.loot_pool (JSON array)
- [ ] All enemies dead → status='ended', ended_reason='victory'
- [ ] loot_pool persisted before combat closed
- [ ] No existing tests broken: python -m pytest tests/test_phase8_combat.py -v
- [ ] Test: kill enemy with loot_table_key → loot_pool populated
- [ ] Test: drop_chance=0.0 → loot_pool empty
- [ ] Test: all enemies dead → ended_reason='victory'
- [ ] resolve_enemy_loot mocked in tests (no real DB loot tables needed in CI)

FILES TO MODIFY (only if gaps confirmed):
- backend/app/services/combat_service.py  (GAP A-D)

DO NOT modify:
- game_engine.resolve_enemy_loot() — reuse as-is
- game_config_loot_tables / game_config_loot_entries schema
- backend/prompts/system_prompt.txt
- Phase 7 roll card system


=====================AFTER REPORT===========
Oto co z tego wynika:

### Dokument `step_7.1_loot_popup.txt`
Treść pliku to w praktyce **ten sam audyt backendu co krok 6.1** (loot_pool, `resolve_enemy_loot`, `dead`, victory). To **już jest zaimplementowane** w `combat_service.py` — tu nie było nic do dopisywania po stronie API.

### Brakujący element „popup”
W **`combat_panel.js`** były `_showLootPopup` / `_showLootPopupAsync`, ale **nigdzie nie były wywoływane** — łupy szły tylko na pełnoekranowy ekran końca walki (`_showEndScreen`).

**Zrobione:**

1. **`showVictoryAfterNarration`** (ścieżka po narracji SSE, zwycięstwo z odroczonym UI) jest **`async`**: najpierw **`await this._showLootPopupAsync(drop)`** (kolejność: `pendingLoot` z ostatniego ataku, inaczej `this._state.loot_pool`), potem **`_pushLoot(drop)`**, na końcu **`showVictory`**.

2. **`_onAttack`** — gdy walka kończy się od razu (**`!victoryNarrationFirst`**, bez czekania na SSE): przed **`showVictory`** — jeśli jest pula łupów, **`await this._showLootPopupAsync(pool)`** z `combat_state.loot_pool` albo z `data.loot`, oraz **`this._accumulatedLoot = pool.slice()`**, żeby ekran końca zgadzał się z modalem.

3. **`actions.js`** — przy `[DONE]` i przy „stream ended without `[DONE]`” jest **`await window.combatPanel.showVictoryAfterNarration(...)`**, żeby modal zdążył się zamknąć przed dalszym przebiegiem.

4. Nagłówek modala: **„Łupy z pokonanych”** (zamiast samego „Łup”).

### Notion
Aktualizacja strony nie przeszła (Notion: „No matches found” — rozjazd treści względem `old_str`). Możesz dopisać krok 7.1 ręcznie albo dać znać, żebym ponowił po świeżym `notion-fetch`.

### Docker
Zmiany **tylko w plikach JS** — **restart/rebuild kontenera nie jest konieczny**, jeśli serwujesz statyczne pliki z dysku w dev (wystarczy odświeżenie). Przy obrazie Dockera, który **kopiuje** front przy buildzie, potrzebny jest **rebuild obrazu** (albo podmiana plików w volume), żeby produkcja dostała nowy JS.