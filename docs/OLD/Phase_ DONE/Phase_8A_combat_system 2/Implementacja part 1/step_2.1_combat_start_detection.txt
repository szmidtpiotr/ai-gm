SAFETY CHECK RESULT: ✅ MECHANISM ALREADY EXISTS — STEP REDEFINED

Findings from repo (Phase 8A v1, verified 2026-04-24):
- Player input processed in: backend/app/api/turns.py
- Combat detection already exists: _maybe_start_combat_from_gm_tag() in turns.py
- COMBAT_START_RE regex already parses [COMBAT_START:key1,key2] from GM response
- combat_service.py already exists with start_combat() / create_combat_state()
- game_config_enemies table confirmed: key, label, hp_base, ac_base, attack_bonus,
  damage_die, dex_modifier, loot_table_key, drop_chance, is_active
- active_combat table confirmed: id, campaign_id, character_id, round, turn_order,
  current_turn, combatants, status, ended_reason, location_tag, loot_pool
- IMPORTANT: use EXISTING column names in all new code:
    "status"       (not "phase")
    "current_turn" (not "current_actor_id")
    "round"        (not "turn_number")

This step is NOT "implement combat detection from scratch".
It is "audit existing mechanism + fill gaps if any".

---

SAFETY CHECK — run before touching anything:

1. Read the full detection flow:
   grep -n "COMBAT_START_RE\|_maybe_start_combat\|start_combat" backend/app/api/turns.py
   grep -n "def start_combat\|def create_combat\|def initiate_combat" backend/app/services/combat_service.py

2. Check current combatants JSON shape — player and enemy fields.

3. Multi-enemy: does _maybe_start_combat_from_gm_tag() loop over all keys?

4. Invalid key: crash / 500 / silent skip / warning?

5. Player stats in snapshot: all 7 stats present?

STOP — report findings. Implement ONLY what is missing or broken.

---

IMPLEMENTATION — fill gaps only:

GAP A — Invalid enemy_key: log warning, skip, don't crash.
GAP B — Player snapshot: id, type, name, hp_max, hp_current, all 7 stats.
GAP C — Enemy snapshot: dex_modifier from game_config_enemies row.

Do NOT rewrite start_combat() or _maybe_start_combat_from_gm_tag() from scratch.

---

ACCEPTANCE CRITERIA:
- [ ] Detection confirmed end-to-end
- [ ] Invalid key: warning logged, no broken combat state
- [ ] Player: hp_max, hp_current, all 7 stats in combatants JSON
- [ ] Enemy: dex_modifier in combatants JSON
- [ ] Multi-enemy: both in combatants array
- [ ] Tests: python -m pytest tests/test_phase8_combat.py -v
- [ ] Manual: active_combat row verified on 192.168.1.61

FILES (only if gaps confirmed):
- backend/app/services/combat_service.py  (GAP B, GAP C)
- backend/app/api/turns.py                (GAP A)

===========AFTER REPORT ============


Oto co zrobiłem w odpowiedzi na **`step_2.1_combat_start_detection.txt`** (audyt + luki):

### Audyt (przed zmianą)

| Punkt | Stan |
|--------|------|
| Multi-enemy `key1,key2` | Już było: split po `,`, pętla po kluczach. |
| **GAP C** `dex_modifier` w JSON wroga | Już było w `combatants`. |
| **GAP A** nieznany klucz | `raise ValueError` → w `turns.py` łapane jako `Exception` → **`logger.error`**, brak walki. |
| **GAP B** staty gracza | Brak pola **`stats`** (było HP, DEF, inicjatywa). |
| Inicjatywa wroga | **`dex_e_mod = 0`** zamiast wartości z bazy. |

### Zmiany

1. **`combat_service.py`**
   - **`_ability_stats_seven(sheet)`** — `STR`–`CHA` z `sheet.stats` + **`speed`** (domyślnie 30), żeby spełnić „7 pól” z acceptance.
   - **`initiate_combat`**: najpierw zbiera tylko znane szablony z DB; dla nieznanego klucza **`logger.warning`** (`combat_unknown_enemy_key`). Jeśli po filtrze lista pusta → **`ValueError("no valid enemy keys after filtering...")`**.
   - Gracz w `combatants` dostaje **`"stats": ability_stats`**.
   - Inicjatywa wroga: **`roll_d20() + int(er["dex_modifier"] or 0)`** (zamiast 0).
   - Log startu / `narrative` w `log_combat_turn` używa **tylko faktycznie dodanych** wrogów.

2. **`turns.py`**
   - **`ValueError`** z `initiate_combat` → **`logger.warning`** (`combat_gm_tag_rejected`), **`return None`** (bez `error` jak przy nieoczekiwanym wyjątku).

3. **`tests/test_phase8_combat.py`**
   - Asercje: `stats`, `speed`, `dex_modifier` przy starcie.
   - **`test_initiate_combat_skips_unknown_enemy_keys`**
   - **`test_initiate_combat_raises_when_all_enemy_keys_unknown`**

### Pytest (192.168.1.61)

`tests/test_phase8_combat.py`: **26 passed**.

### Docker

Po wdrożeniu backendu w kontenerze: **restart (lub rebuild)**, żeby wczytać `combat_service.py` i `turns.py`.