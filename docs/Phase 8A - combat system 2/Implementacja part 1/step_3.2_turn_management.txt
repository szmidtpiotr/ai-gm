SAFETY CHECK FIRST:

Before implementing, please analyze and report findings:

1. Does get_current_actor / advance_turn already exist?
   grep -n "get_current_actor\|advance_turn\|current_turn\|is_combat_active" backend/app/services/combat_service.py
   grep -n "advance_turn\|current_turn" backend/app/api/turns.py

2. How is "whose turn is it?" currently read?
   active_combat columns are: current_turn (TEXT), round (INTEGER), turn_order (TEXT/JSON)
   NOT current_actor_id / current_actor_index / turn_number — use EXISTING names.

3. Is turn advancement already triggered anywhere?
4. Race condition protection — what does the codebase already use?
5. Does is_combat_active equivalent exist?

STOP — report findings. If logic already exists → audit only, fill gaps.

---

IMPORTANT: column name mapping
  current_actor_id    → current_turn
  current_actor_index → computed from turn_order list (NOT stored)
  turn_number         → round

---

FUNCTIONS TO ADD (if missing):

A — get_current_actor(conn, campaign_id) → str | None
B — advance_turn(conn, campaign_id) → str | None
    - wraps round when last actor reached
    - increments round when next_index == 0
    - wrapped in DB transaction
C — is_combat_active(conn, campaign_id) → bool

DO NOT add new columns to active_combat.

---

ACCEPTANCE CRITERIA:
- [ ] get_current_actor returns current actor or None
- [ ] advance_turn updates current_turn + round, wraps correctly
- [ ] round increments on full cycle
- [ ] is_combat_active returns bool
- [ ] DB writes in transaction
- [ ] Tests: python -m pytest tests/test_phase8_combat.py -v
- [ ] Test: 3 advance_turn calls → [0]→[1]→[0+round+1]→[1]

FILES: backend/app/services/combat_service.py only



==================AFTER REPORT=========================

### Zrobione (krok 3.2)

**`combat_service.py`**

1. **`get_current_actor(conn, campaign_id) -> str | None`**  
   Zwraca `current_turn` z `get_active_combat` albo `None`. Parametr **`conn` jest ignorowany** (walka jest na `COMBAT_DB_PATH`, jak w reszcie serwisu).

2. **`is_combat_active(conn, campaign_id) -> bool`**  
   `True`, gdy jest aktywna walka (`get_active_combat` nie jest `None`). **`conn`** — ten sam zabieg co wyżej.

3. **`advance_turn`** — jedna funkcja, dwa sposoby wywołania:
   - **Dotychczas:** `advance_turn(campaign_id: int) -> str` — bez zmian zachowania (dalej **`ValueError`**, gdy brak walki).
   - **Z dokumentu:** `advance_turn(conn, campaign_id)` — `conn` może być `None` lub `sqlite3.Connection` (na przyszłość); przy braku aktywnej walki **zwraca `None`**, nie rzuca.

Logika tur jest w **`_advance_turn_impl`** (dawne ciało `advance_turn`); `turns.py` / `combat.py` dalej wołają **`advance_turn(campaign_id)`**.

### Testy

W **`test_phase8_combat.py`** dodany jest **`test_step_32_turn_helpers`** (helpers + `advance_turn(None, 1)`).

Na **.61**: **`tests/test_phase8_combat.py` — 27 passed**.

### Docker

Po wdrożeniu backendu w kontenerze: **restart / rebuild**, żeby wczytać zmieniony `combat_service.py`.