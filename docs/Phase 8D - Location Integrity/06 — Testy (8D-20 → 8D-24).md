Jesteś programistą Python/pytest pracującym nad projektem ai-gm.

Repozytorium: github.com/szmidtpiotr/ai-gm
Branch: phase-8d-location-integrity
Plik docelowy: tests/test_phase8d_location_integrity.py

ZANIM NAPISZESZ JAKIKOLWIEK KOD:
Najpierw przejrzyj istniejący kod testów i backendu, a potem odpowiedz konkretnie na poniższe pytania:

1. Jak skonfigurowana jest testowa baza danych?
   - Czy jest in-memory SQLite?
   - Czy jest fixture w conftest.py?
   - Czy testy używają osobnego DB od production data/ai_gm.db?

2. Czy są już fixtures dla sesji/postaci, które mogę użyć?
   - Jeśli tak, wskaż ich nazwy i pliki.
   - Jeśli nie, powiedz to wprost.

3. Czy testy mockują wywołania LLM?
   - Jak dokładnie?
   - Czy jest monkeypatch, stub, fake driver, fixture lub patch na llm_service?
   - Żebym nie robił prawdziwych wywołań API w testach.

4. Czy `python3 -m pytest` przechodzi teraz czysto?
   - Zero failed przed moimi zmianami.
   - Jeśli nie masz pewności, powiedz że trzeba to zweryfikować i wskaż potencjalne ryzyko.

5. Czy jest jakaś struktura parametryzowanych testów, którą powinienem zachować?
   - Np. style nazw, markery, helpery, konwencja arrange/act/assert, wzór parametrization.

WAŻNE:
- Jeśli którakolwiek odpowiedź wskazuje na problem, blokadę albo brak gwarancji, to zatrzymaj się i opisz problem ZANIM napiszesz kod.
- Nie zakładaj niczego bez sprawdzenia.
- Preferuj odpowiedź opartą na faktycznie istniejących plikach repo.
- Nie używaj produkcyjnej bazy `data/ai_gm.db` w testach.
- LLM ma być w testach w pełni mockowany.
- Testy mają być deterministyczne i izolowane.

Jeśli nie widzisz blokerów, napisz testy dla Phase 8D:

Test 8D-20: Blokada teleportacji
- Setup: sesja z current_location = 'tavern_hanged_man' (sub of city_varen)
- Intent: move do 'forest_black' (inne makro)
- Assert:
  - ValidationResult.allowed == False
  - session.current_location_id nie zmieniona
  - location_integrity_log ma nowy wpis z reason_blocked

Test 8D-21: Dozwolony ruch sub → sub ten sam parent
- Setup: sesja z 'tavern_hanged_man' (sub of city_varen)
- Intent: move do 'market_square' (sub of city_varen)
- Assert:
  - ValidationResult.allowed == True
  - session.current_location_id zaktualizowane
  - location_integrity_log BEZ nowego wpisu

Test 8D-22: Admin wyłącza flagę — blokada odpada
- Setup: sesja z session_flags = {"location_integrity_enabled": 0}
- Intent: ta sama teleportacja co test 8D-20
- Assert:
  - Walidacja pominięta
  - session.current_location_id zaktualizowane

Test 8D-23a: GM tworzy nową lokalizację dynamicznie
- GM response JSON z action='create', target_label='Grota za Wodospadem'
- Assert:
  - Nowy wpis w game_locations (klucz = slug target_label)
  - session.current_location_id = nowa lokalizacja

Test 8D-23b: Fuzzy match — reuse istniejącej lokalizacji
- Istnieje 'Karczma Pod Wisielcem' w game_locations
- Intent: target_label = 'karczma pod wisielcem' (lowercase)
- Assert:
  - Brak nowej lokalizacji w DB
  - Dopasowano istniejącą (rapidfuzz score >= 80)

Po napisaniu testów:
- Nie dodawaj na ślepo zmian produkcyjnych.
- Jeśli trzeba, zaproponuj minimalne poprawki do fixture lub helperów.
- Na końcu krótko podsumuj:
  - co znalazłeś,
  - czy są blokery,
  - czy testy można bezpiecznie dopisać.

Dodatkowo:
- Utrzymuj obecny styl repo.
- Jeśli istnieje pattern parametrized tests, zachowaj go.
- Jeśli trzeba użyć helperów, dodaj je minimalnie i lokalnie w tym samym pliku testowym, chyba że repo ma już wspólne helpery.