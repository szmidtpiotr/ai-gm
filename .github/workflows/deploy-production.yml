name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to PROD (.62)
    runs-on: self-hosted

    steps:
      - name: Deploy
        working-directory: /home/piotrszmidt/ai-gm
        run: |
          git pull --ff-only origin main
          ./scripts/deploy_prod.sh
